int[] numbers = { 15, 8, 25,-3, 3, 12,0 };

int max = numbers[0];
int min = numbers[0];

for (int i = 1; i < numbers.Length; i++)
{
    if(numbers[i] > max)
    {
        max = numbers[i];
    }
    if(numbers[i] < min) 
    {
    
        min = numbers[i];

    }
}
Console.WriteLine(max);
Console.WriteLine(min);

